package vidya;

public class Config {
    int windowMonitor;
    int windowSamples;
    int windowWidth;
    int windowHeight;
    int windowSwapInterval;

    Window.Mode windowMode = Window.Mode.WINDOWED;

    boolean debugIde;
}
